# Third-party notices

This public baseline has no runtime package dependencies and no third-party source library bundled into the application. It uses built-in browser APIs for image processing and Node.js built-ins for tests.

The root MIT License applies only to original project files. It does not replace the license or attribution requirements of any third-party material added later. Preserve the original license and attribution alongside any future dependency or reused source.
